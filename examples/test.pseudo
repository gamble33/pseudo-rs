PROCEDURE Main
    DECLARE Count : INTEGER
    DECLARE I : INTEGER

    Count <- 100
    WHILE Count > 0
        OUTPUT Count
        Count <- Count - 1
    ENDWHILE

    FOR I <- 0 TO 100
        OUTPUT I
    NEXT I
ENDPROCEDURE

